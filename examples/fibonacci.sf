# This is a simple SynthFlow program
# It demonstrates basic syntax

fn fibonacci(n: Int) -> Int {
    if n <= 1 {
        return n
    }
    return fibonacci(n - 1) + fibonacci(n - 2)
}

fn main() -> Int {
    let result = fibonacci(10)
    print("Fibonacci of 10 is: " + result)
    return 0
}